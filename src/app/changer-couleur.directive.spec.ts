import { ChangerCouleurDirective } from './changer-couleur.directive';

describe('ChangerCouleurDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangerCouleurDirective();
    expect(directive).toBeTruthy();
  });
});
